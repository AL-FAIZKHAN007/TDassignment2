# TDassignment2
TASK1: Check if a Number is Even or Odd.
        -Asking user to enter a number.
        -Using 'if-else' statement, to check the number is Even or Odd.
        -Printing the output.


TASK2: Sum of Integers from 1 to 50 Using a Loop.
        -0(zero) initialized to a variable sumOfNumber, it will be increment inside the loop.
        -Used 'for loop' to iterate the numbers from 1 to 50.
        -Incrementing the variable sumOfNumbers by iterated numbers.
        -Printing the final sum of numbers.